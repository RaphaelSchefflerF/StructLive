import { listRegistry } from '@/app/estruturas/lista/config';
import { PilhaRegistry } from '@/app/estruturas/pilha/config';

export const masterRegistry = {
  lista: listRegistry,
  pilha: PilhaRegistry,
};
